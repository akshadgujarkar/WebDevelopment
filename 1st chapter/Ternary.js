const age = 24
const VotingAge = 18

const ans = (age>VotingAge) ? 'eligible for vote' : 'not eligible for voting'
console.log(ans);

age2 = 24
console.log((age2>VotingAge)?'eligible for voting' : 'not eligible for voting');


const gender = 'm'
console.log(`${(gender.toLowerCase()=='m') ? 'Male' : 'Female'}`);